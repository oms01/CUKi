
discord:
  webhook: ${DISCORD_WEBHOOK:}

kakao:
  client_id: ${CLIENT_ID:}
  redirect_uri: ${REDIRECT_URI:}

fcm:
  file_name: firebase-service-account.json

jwt:
  secret: ${JWT_SECRET:}

scheduler:
  detector:
    thread_count: ${DETECTOR_THREAD:5}
    interval: ${DETECTOR_INTERVAL:60}